/**
 * Default value for how many milliseconds to wait between retries.
 */
export const defaultRetryDelayMs = 200;

/**
 * Default number of attempts to retry we should take before giving up.
 */
export const defaultRetries = 2;

/**
 * Default page size if none is provided.
 */
export const defaultGlobalPageSize = 100;

/**
 * Default sort direction if none is provided.
 */
export const defaultSortDirection = "asc";
